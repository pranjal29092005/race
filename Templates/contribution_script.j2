array<uint> indices;
{% for i in range(analysisCount) %}
indices.insertLast({{ i }});
{% endfor %}
string errorMessage;
if (!ContributionByV2("GroupBy", @indices, errorMessage)) {
    Send("Error!", errorMessage);
    return;
}
